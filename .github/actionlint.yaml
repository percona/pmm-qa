# actionlint's embedded shellcheck stays suppressed in this phase: the ~120
# findings in `run:` blocks are a separate cleanup from the workflow-schema ones.
# Patterns are regexes matched against the reported message.
paths:
  .github/workflows/**:
    ignore:
      - 'shellcheck reported issue in this script'
