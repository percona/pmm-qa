# actionlint's embedded shellcheck now runs; nothing is suppressed here.
#
# It is invoked with SHELLCHECK_OPTS='-S warning' by both entry points, which
# holds `run:` blocks to exactly the bar the repo's own .sh files under
# qa-integration/ and terraform/ are held to. Raising it further means taking
# on shellcheck's info and style tiers (55 of them SC2086 quoting) across every
# workflow, which is a separate decision from this one.
