# actionlint's embedded shellcheck runs at its own default severity -- info and
# style included -- and nothing is suppressed here. `run:` blocks are shell and
# are held to the same bar as the repo's .sh files.
#
# Seven findings carry an inline `# shellcheck disable=SC2016` with the reason
# next to them: five are `bash -c '...'` polls where the inner shell must do the
# expanding, two are a sed that substitutes a literal on purpose.
