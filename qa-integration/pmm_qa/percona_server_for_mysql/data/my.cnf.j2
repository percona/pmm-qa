[mysqld]
plugin-load-add=auth_native_password.so
# Enable all INNODB_METRICS counters (disabled by default) so the MySQL InnoDB Details dashboard panels have data
innodb_monitor_enable=all
userstat=1
plugin-load-add = query_response_time.so
query_response_time_stats = ON
