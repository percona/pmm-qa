# .claude/skills/add-ha-test/SKILL.md — a cloud session cannot reach a LoadBalancer by bare IP

- Added: 2026-09-23
- Applies to: add-ha-test "Running them" cloud-session overrides
- Evidence: a test calling `https://<LKE LoadBalancer IP>/v1/server/readyz` failed with ECONNRESET (curl exit 35) through the sandbox egress proxy, while the same LB via its `*.ip.linodeusercontent.com` hostname returned 200; a 12-minute run was lost before checking curl.
- Proposed change: add to the cloud-session paragraph that the egress proxy resets TLS to bare IPs, so a test addressing the LB by IP needs an untracked copy mapping it to the Linode hostname for a sandbox run — check with curl by IP vs hostname first.
