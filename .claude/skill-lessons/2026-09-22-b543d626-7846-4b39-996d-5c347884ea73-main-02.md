# .claude/skills/zephyr/SKILL.md — without RELAY_KEY, ask for the folder XML export instead of trying the browser

- Added: 2026-09-22
- Applies to: target only
- Evidence: With `RELAY_KEY` absent every broker call returned 403 `forbidden` and the in-app browser could not render the Zephyr Scale Connect iframe (anonymous Atlassian session); the user's folder export `atm-exporter.xml` carried key, status, folder, linked Jira keys, precondition, objective and full step text, and parsed directly with `xml.etree.ElementTree` over `testCase` elements.
- Proposed change: Add a fallback under "Access path": when `RELAY_KEY` is unset, ask the user for the Zephyr folder XML export before attempting any browser route, noting that it is read-only — status changes still need the relay or the Zephyr UI.
