# .claude/skills/linode-docker-provisioning/SKILL.md — playwright runner compose collides with nginx on 443 (recurrence)

- Added: 2026-09-25
- Applies to: target only
- Evidence: Replaying `runner-e2e-tests-playwright.yml` on the VM, `e2e_tests/docker-compose.yml` failed with `failed to bind host port 0.0.0.0:443/tcp: address already in use` and readyz timed out; an untracked `sed 's/"443:8443"/"8443:8443"/' docker-compose.yml > docker-compose.local.yml` came up and the suite ran on the default `http://localhost/` via port 80.
- Proposed change: In section 5, give the concrete remap recipe (untracked compose copy publishing 8443 instead of 443) for both the codeceptjs and playwright runner composes.
