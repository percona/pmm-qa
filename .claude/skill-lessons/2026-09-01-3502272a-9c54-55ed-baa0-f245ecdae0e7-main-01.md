# .claude/agents/investigator.md — read `repos` before the first GitHub MCP call, not when something breaks

- Added: 2026-09-01
- Applies to: target only
- Evidence: An FB investigation opened with `actions_list`/`list_pull_requests`, both of which overflowed the result cap, and cost three failed `python3` parses ending in `TypeError: string indices must be integers` — the exact error, the per-call nesting table and the fix are already written down in `.claude/skills/repos/SKILL.md`, which the Knowledge table lists but nothing said to read before the first call.
- Proposed change: In the Knowledge table, mark `repos` as read-first (before any GitHub MCP listing/log call), the way the workflow already orders `fb-tests` and `linode-docker-provisioning` at the steps that need them.
