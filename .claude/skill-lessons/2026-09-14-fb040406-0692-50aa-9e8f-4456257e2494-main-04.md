# .claude/skills/qa-code-review/SKILL.md — do not prescribe deleting an enumeration entry made unreachable by a distant guard

- Added: 2026-09-14
- Applies to: .claude/skills/qa-code-review/SKILL.md
- Evidence: A pre-existing (blue) thread asked for `'latest-tarball'` to be dropped from a `use_encrypted_client_config` list, because a new assert 200 lines above made that arm unreachable. The author applied it and then reverted it (https://github.com/percona/pmm-qa/pull/1429#discussion_r4006896819): the list states a local property — which client versions support encrypted config — and deleting the entry made that property depend on a guard far away, so a later harmonisation of the two checks would silently stop enabling it. The entry is back on main at `qa-integration/pmm_qa/tasks/install_pmm_client.yml:206`. The unreachability analysis itself was never disputed.
- Proposed change: Extend check 7's confirm-don't-prescribe limit to an enumeration entry whose deadness depends on a guard elsewhere in the file: a list documenting a local property stays semantically complete, so state the redundancy and ask the author, rather than posting a suggestion block that deletes the entry.
