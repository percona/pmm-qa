# .claude/skills/linode-ha-provisioning/SKILL.md — login_attempt psql snippet fails because GF_DATABASE_HOST carries the port

- Added: 2026-09-23
- Applies to: target only
- Evidence: The skill's `psql -h "$GF_DATABASE_HOST"` failed with `could not translate host name "pmm-ha-pg-db-ha.pmm.svc.cluster.local:5432"`, and `kubectl exec statefulset/pmm-ha` without `-c` needed the container name `pmm-ha`.
- Proposed change: Use `-h "${GF_DATABASE_HOST%%:*}"` (and `-c pmm-ha`) in the lockout and inspection snippets.
