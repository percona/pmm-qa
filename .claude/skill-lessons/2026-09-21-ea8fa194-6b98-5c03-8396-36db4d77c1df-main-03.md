# .claude/skills/linode-docker-provisioning/SKILL.md — pin executablePath for any ad-hoc Playwright script, not only a scratch config

- Added: 2026-09-21
- Applies to: target only
- Evidence: Step 4 documents `executablePath: '/opt/pw-browsers/chromium'` only for the scratch `playwright.config.ts` used to run the repo's own suite. A standalone probe script calling `chromium.launch(proxyLaunchOptions())` died with `Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/...` plus the "run npx playwright install" banner; `/opt/pw-browsers` holds only `chromium`, `chromium-1194`, `chromium_headless_shell-1194`.
- Proposed change: State in step 4 that the `executablePath: '/opt/pw-browsers/chromium'` pin applies to every Playwright launch from this environment, ad-hoc scripts included — not only the scratch suite config — since the bundled-browser revision Playwright resolves is not the one installed.
