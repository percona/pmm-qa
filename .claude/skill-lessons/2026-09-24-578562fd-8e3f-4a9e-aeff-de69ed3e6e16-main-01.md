# CLAUDE.md — Recalling prior work: search local transcripts and artifacts, not only desktop-app sessions

- Added: 2026-09-24
- Applies to: all sessions
- Evidence: Asked about a guide drafted in an earlier session, `search_session_transcripts` returned nothing because that session ran from the IDE/CLI; `Artifact action:list` found the published page and `grep -lis` over `~/.claude/projects/*/*.jsonl` found the drafting transcript and where it stopped.
- Proposed change: When a user asks about work from an earlier session, check `Artifact action:list` and grep the local `~/.claude/projects/*/*.jsonl` transcripts alongside the desktop-app transcript search before concluding nothing exists.
