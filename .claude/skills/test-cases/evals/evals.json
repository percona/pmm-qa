{
  "skill_name": "test-cases",
  "evals": [
    {
      "id": 1,
      "prompt": "/test-cases PMM-15379",
      "expected_output": "Facts checked 2026-09-23 against percona/pmm#5923 at 22d785b, still open; re-check them if it has moved. Deep. Cases: a Viewer supplying X-Proxy-Admin through the data source proxy is refused with the snapshot list unchanged; the vmproxy read-only gate holds with access control disabled; normal dashboard, variable and Explore traffic is never refused, on a lane whose job both provisions the estate and selects the test's tag; a Viewer holding an empty-filter role still reads every series. Routes are enumerated from the configuration, not from How to test. No route-shape or admin-diagnostics case, because api-tests/accesscontrol covers them from the Jenkins API-test job. Findings name the api/v1/targets move during PR review and the feature-build image requirement. Grade by mechanisms covered: access-control-off gate; forged trusted inputs (X-Proxy-Admin, X-Proxy-Filter); full-access direction; route enumeration; route shapes subtracted as api-tests coverage.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A case sends X-Proxy-Admin from a Viewer and asserts 403 with an unchanged snapshot list",
        "A case runs with access control disabled and asserts the gated sub-paths return 403",
        "The notes enumerate the routes reaching vmproxy or VictoriaMetrics from nginx locations and Grafana routes, including the Grafana alerting routes and the paths that reach VictoriaMetrics without vmproxy; each is a case, a ledger row, or a stated drop",
        "Any HA case asserts a status code that differs between the broken and fixed build, not only an empty result",
        "The estate sweep names a lane only where one job both provisions the technology and selects the test's tag; the rest are infra gap or Manual with the workflow search recorded in the notes; a blanket Manual with no recorded search fails",
        "The full-access direction (empty-filter Viewer sees every series the admin sees) is asserted in some case, or dropped with a ledger row citing the exact api-tests assertion",
        "No case repeats the four route shapes or admin diagnostics; the draft cites api-tests/accesscontrol as coverage and names how its run was confirmed",
        "Findings state that api/v1/targets is 403 for a Viewer, contradicting How to test",
        "Every implementation Evidence names a repository path with a function or line",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 2,
      "prompt": "/test-cases PMM-15420",
      "expected_output": "Facts checked 2026-09-23. Mechanisms: the HA badge stays Healthy across the token rotation deadline (HA lane); the session recovers past the rotation deadline through 401, rotate and replay; an unrecoverable session redirects to login once; server restart does not log the user out (Manual); two tabs past the deadline both recover (Manual). Findings reject the AI-triage fetchStatus root cause, correct rotated_at - 150 to a value above 3600 seconds (token_rotation_interval_minutes = 60), and note that docker exec psql is wrong for HA where Grafana uses the shared PostgreSQL. Grade by mechanisms covered, not by the number of cases.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A Finding rejects the fetchStatus === 'paused' triage hypothesis with a code location",
        "A Finding corrects the How to test offset from 150 to a value above 3600 seconds",
        "A case captures the user_auth_token row id at setup and restores rotated_at in cleanup",
        "A case asserts exactly one rotate call per expiry and no navigation to /login",
        "The HA case waits on the 401, rotate and replayed responses, not on a duration",
        "A case waits past pmm-managed's one-minute auth cache before asserting the 401, and names an in-app poll rather than a page load as the trigger",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 3,
      "prompt": "/test-cases PMM-15138",
      "expected_output": "Facts checked 2026-09-23. RBAC cases deny the mutation at the API boundary and verify unchanged state, with a Finding that a Viewer reading alert templates contradicts the documentation and PMM-T1991, rather than a case that asserts either side as fact. Existing coverage is classified: PMM-T2235 asserts the 403 but not the unchanged state, so it is Extend; PMM-T2236 (Automated) asserts the disputed Viewer read as fact, which the Finding names. Grade by mechanisms covered: denied mutation with unchanged state for POST, PUT and DELETE; the disputed Viewer read recorded as a Finding, not asserted.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A Finding records that GET /v1/alerting/templates answers 200 to a Viewer while the permission matrix and PMM-T1991 (rule-templates page refused) say Viewer cannot view templates",
        "No case asserts the disputed Viewer read as the expected behavior",
        "Each denied-mutation case asserts the API status and the unchanged persisted state",
        "PMM-T2235 is classified Extend, with the unchanged-state assertion added to its flow rather than a duplicate case",
        "The Finding names PMM-T2236 as existing automation that asserts the disputed Viewer read",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 4,
      "prompt": "what should I test for PMM-14951?",
      "expected_output": "Facts checked 2026-09-23. Focused or Standard depth. No new case for the =true mechanism: e2e_tests/tests/variableParsing.test.ts (PMM-T2206, Automated) follows the ticket's steps and asserts the URL carries no timezone=...=true.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The notes record Focused or Standard depth with its trigger; not Deep",
        "PMM-T2206 in e2e_tests/tests/variableParsing.test.ts counts as coverage for the =true mechanism unless the notes show a concrete way it passes on the broken build; no new case repeats it",
        "The Covered row states what the assertion sees on the base branch",
        "The history step includes a query on the ticket's own symptom",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 5,
      "prompt": "/test-cases PMM-15242",
      "expected_output": "Facts checked 2026-09-23. Zero new cases for the missing usage output: cli/tests/generic.spec.ts asserts the usage text on stdout and TestNoCommandPrintsUsage in percona/pmm admin/cli runs on the pull request. The Go test passes --server-url, which skips the pmm-agent lookup, so running pmm-admin with no arguments while pmm-agent is stopped is untested.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The draft proposes no new case for the fixed panic and cites both tests as coverage, naming how the Go test's run was confirmed",
        "A Finding raises or rules out, with a code location, the no-agent path the Go test bypasses with --server-url",
        "If a case is proposed for the no-agent path it is Automation candidate — infra gap naming the missing lane, not Manual",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 6,
      "prompt": "is PMM-14824 already covered?",
      "expected_output": "Facts checked 2026-09-23. The implementation is not under PMM-14824: the comments place it in PMM-14012/PMM-14694 (percona/pmm#4900 removed api/platform). Zero new cases: e2e_tests/tests/portalRemoval.test.ts (PMM-T2163) expects 404 for an authenticated admin.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The draft finds the implementation through the ticket keys named in the comments and says which keys it searched",
        "The draft proposes no new case and cites PMM-T2163 in e2e_tests/tests/portalRemoval.test.ts",
        "A Finding raises or rules out, with a code location, whether unauthenticated or under-privileged callers still get 401 or 403 from leftover auth rules for /v1/platform",
        "A Finding records that the fix version (3.7.0) conflicts with the comments placing the removal in 3.8.0",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 7,
      "prompt": "/test-cases PMM-15302",
      "expected_output": "Facts checked 2026-09-23. A weak ticket whose scope moved during review. No new case for the non-AMI refusal: api-tests assert ChangeSSHKey is refused off AMI, UI unit tests in Settings.test.tsx cover the tab visibility, and PMM-T85 exercises the AMI tab but skips on non-AMI lanes. The backend now also refuses OVF; open PMM-15430 plans removing OVF, while docs on main still describe the OVA appliance.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "No new case repeats the non-AMI refusal the api-tests and UI unit tests cover, and the draft names how their runs were confirmed; any AMI-side case says why the unit tests' mocked version does not cover it",
        "A Finding states the scope moved from removing the SSH tab in HA to showing it only on AMI",
        "A Finding states the backend now also rejects OVF and names the conflict with the OVF/OVA docs on main or open PMM-15430",
        "No expected behaviour is invented to fill the one-line description",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 8,
      "prompt": "Plan QA for PMM-14670, it's a one-line QAN retention fix",
      "expected_output": "Facts checked 2026-09-23. Deep despite one changed file: the partition purge is HA-only and deletes data. No new Zephyr case: PMM-T2295 covers the HA retention outcome. qan-api2/db_test.go seeds only date partitions, so it passes on the broken build and is not coverage. The right layer is a product test: a non-date partition in TestDropOldPartition, which qan-api2.yml runs with ClickHouse on every pull request.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The notes record Deep depth, triggered by HA and data deletion, not by the size of the diff",
        "The draft does not count qan-api2/db_test.go as Covered and says why it passes pre-fix",
        "A Recommend Finding asks for a non-date partition in TestDropOldPartition",
        "No new Zephyr case duplicates PMM-T2295",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 9,
      "prompt": "/test-cases PMM-15112",
      "expected_output": "Facts checked 2026-09-23. A weak ticket whose only strong signal is history: PMM-14892 was the same livereload failure. One automated case: loading PMM makes no request to localhost:35729. A Finding notes the CI guard the refinement asked for was not built.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The history step finds PMM-14892 through a query on the ticket's symptom",
        "One case asserts that loading PMM sends no request to port 35729, marked Needs automation with a named lane",
        "A Finding or Recommend line covers the requested CI string check",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 10,
      "prompt": "/test-cases PMM-14787",
      "expected_output": "Facts checked 2026-09-23. Deep. The docs in percona/pmm#5963 say VictoriaMetrics removes a whole month partition at a time; VictoriaMetrics' own docs say the cycle is one day for retention up to 30 days, and queries skip data older than the retention. The expected result follows VictoriaMetrics, and the ticket's acceptance scenario is kept with a bound of retention plus one day. The skill-authored PMM-T2290 to PMM-T2295 already exist for this ticket.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A Finding labels the pull request's docs claim implementation-derived and cites VictoriaMetrics' own docs or source against it",
        "The acceptance scenario is kept, not dropped as upstream behaviour, and its bound is the retention period plus one day",
        "The Zephyr check finds PMM-T2290 to PMM-T2295 and proposes no duplicate",
        "A Finding records that PMM-T2290 and PMM-T2292 describe opposite UI decisions; any case that depends on it is Draft",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 11,
      "prompt": "what should we verify for PMM-15191? change agent returns a 500",
      "expected_output": "Facts checked 2026-09-23. Before the fix, executeAgentChange commits the change and then fails the agent-type check, returning 500 after the write. Cases: a change to the wrong agent type is refused before anything is written; an accepted change alters only the requested field and reaches the exporter. The unchanged-state check compares the decoded fields the request could write: a full-row snapshot breaks because updated_at, re-encrypted credentials and status change on every write.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A case sends the change to the wrong agent type and asserts refusal with the fields the request could write unchanged",
        "The unchanged-state oracle names the fields it compares rather than a whole-row or whole-listing snapshot",
        "The regression case states base behaviour, fixed behaviour, and the assertion that tells them apart",
        "An accepted change is asserted to alter only the requested field and to reach the exporter's running configuration",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 12,
      "prompt": "/test-cases PMM-15161",
      "expected_output": "Facts checked 2026-09-23. Focused. mysqld_exporter's own unit and live tests (run across the MySQL matrix in its CI) cover the corrected unsigned detection. What remains at PMM level is the auto-increment alert on an UNSIGNED ZEROFILL column, with the pmm-client version that ships the exporter in Preconditions.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The exporter's own tests are subtracted as coverage with how their run was confirmed",
        "At most one PMM-level case, at the alert or panel that consumes the metric",
        "The pmm-client version carrying the fixed exporter is in Preconditions",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 13,
      "prompt": "/test-cases PMM-15363",
      "expected_output": "Facts checked 2026-09-23. A dashboard value-mapping fix. The deterministic oracle is the dashboard model (its JSON or the Grafana dashboard API) or a seeded zero series, not a live panel whose load may happen to be zero. The existing No Data checks in the page objects trip only when load is 0 by chance, so they are Extend. Panels that still map 0 to No Data keep doing so.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The oracle reads the dashboard model or seeds a zero series; no case depends on live load being zero",
        "The existing No Data checks are classified Extend, not Covered",
        "No case carries a side-effect row or Cleanup line it does not need",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 14,
      "prompt": "/test-cases PMM-14984",
      "expected_output": "Facts checked 2026-09-23. An upgrade from 3.6 to 3.7.0 fails its database migration because the role was created under trust authentication; base code has no ALTER USER branch, so the 28P01 retry loops. The case seeds a role the way 3.6 created it, upgrades to the fixed build, and asserts the upgrade completes and the server reads and writes. HA is excluded by code. No pmm-qa GitHub workflow runs a docker-way upgrade; Jenkins does.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The case seeds pre-upgrade state created the way 3.6 created it, not a fresh install",
        "The draft states base behaviour (3.7.0 loops on 28P01), fixed behaviour, and the distinguishing assertion",
        "The lane is the Jenkins upgrade job or the case is Automation candidate — infra gap naming it",
        "Cleanup, if present, discards the upgraded environment rather than claiming to restore it",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 15,
      "prompt": "/test-cases PMM-15200",
      "expected_output": "Facts checked 2026-09-23. Existing PMM-T2255 (cli pmm-client-docker spec, integration-cli-tests.yml) asserts Connected and a new source port after a silent drop, but no fresh metric sample. It is Extend: add a fresh-sample assertion after one reconnect cycle. A newer client against a pre-3.9 server is raised or ruled out.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "PMM-T2255 is classified Extend with a fresh-sample assertion, not duplicated",
        "The fresh sample is bounded by the reconnect cycle's own constants, not a fixed sleep",
        "The client/server version pair is raised or ruled out with a reason",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 16,
      "prompt": "/test-cases PMM-15031",
      "expected_output": "Facts checked 2026-09-23. A cross-repository change: percona/grafana#921 changes the email templates and percona/pmm#5829 adds the logo to the docs site. The oracle depends on the docs-site deployment, not on the PMM build. The alert-notification email is in scope, though How to test checks only the invite. No Playwright mail helper exists.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "A Finding states the logo depends on the docs-site deployment and merge order",
        "The alert-notification email is in scope, not only the invite",
        "Any automated case is Automation candidate — infra gap naming the missing mail helper",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    },
    {
      "id": 17,
      "prompt": "/test-cases PMM-14014",
      "expected_output": "Facts checked 2026-09-23. Focused. A Percona-only branch in the Grafana help menu shows Open source linking to grafana.com. Zero cases, or one Low read-only check.",
      "files": [],
      "expectations": [
        "scripts/check_draft.py reports no issues on the draft, and each case reads as plain steps a non-QA reader can follow without the Data column",
        "The notes record Focused depth",
        "At most one Low case",
        "No case carries a side-effect row or a Cleanup line",
        "A second run on the same prompt covers the same failure mechanisms; case count and grouping may differ"
      ]
    }
  ]
}
