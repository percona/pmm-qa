# .claude/skills/codeceptjs-migration/branch-workflow.md — Locate the expected_test_jobs counter with a grep instead of reasoning about the edit

- Added: 2026-08-24
- Evidence: `branch-workflow.md` already requires stating the before/after number rather than reasoning about the delta, and that instruction paid off on row 4 - it forced the question "which workflow owns the counter?" instead of "did I add a job?". The final gate then made it mechanical: the counter is a single literal in `nightly-e2e-tests-matrix.yml` with a comment naming its three consumers, and the migration edited only `e2e-tests-matrix.yml`, so nightly consumers were 3 before and 3 after. One `grep -rn expected_test_jobs .github/workflows/` settles the question in one command and is immune to the framing that gets it wrong.
- Proposed change: In `branch-workflow.md` section Workflow coverage, name the command alongside the existing requirement: locate the counter with `grep -rn expected_test_jobs .github/workflows/`, confirm which workflow owns it, then state the before/after count of that workflow's consumers.
