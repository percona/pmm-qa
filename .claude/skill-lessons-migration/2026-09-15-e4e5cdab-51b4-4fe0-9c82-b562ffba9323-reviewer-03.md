# .claude/skills/codeceptjs-migration/locator-fix.md - a Grafana panel locator run against the top document returns 0 and reads exactly like a dead locator

- Added: 2026-09-15
- Applies to: `.claude/skills/codeceptjs-migration/locator-fix.md`, and the MCP locator pass in `run.md` section 4
- Evidence: Verifying row 10's seven PMM Health panels through MCP cost three wasted calls because the panels render inside `#grafana-iframe`. A raw `page.locator('section[data-testid=...]')` against the top document returns count 0 for every one of them, while the page title is correct and the body text renders the sidebar - so the failure presents as "this locator is wrong" rather than "you are in the wrong frame", which is the diagnosis that sends a correct locator back to the writer. The POM is already right: `builders.panelByExactName` roots at `base.page.ts:56 grafanaIframe()`, and inside the frame all 17 panel sections resolve with each of the seven targets at count 1.
- Proposed change: State in `locator-fix.md` that any dashboard locator is verified through `grafanaIframe()`, and that a count of 0 on a dashboard panel is presumed to be a frame-scoping mistake until the same locator has been tried inside the frame. A zero count against the top document is not evidence about the locator.
