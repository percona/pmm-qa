# .claude/skills/codeceptjs-migration/branch-workflow.md — Read the last migration's actual workflow diff before arguing a coverage decision

- Added: 2026-08-24
- Evidence: The runner and the initial gate disagreed on row 4's coverage edit, and prose on both sides could not settle it. One `git show` of the last migration to land on `main` did: that commit made *both* a PR-matrix append and a new nightly entry, and the reason was visible in the same diff - it simultaneously deleted the tag from the nightly CodeceptJS grep, so the nightly edit was preserving coverage rather than adding it. Row 4's source is on one surface, so one edit was warranted. The final gate re-derived the same evidence independently and reached the same verdict.
- Proposed change: In `branch-workflow.md` section Workflow coverage, add: when two coverage shapes are arguable, run `git log -- .github/workflows/` and read the last migration's actual diff before reasoning further - the precedent shows which surfaces the previous retirement moved and why.
