# .claude/skills/codeceptjs-migration/orchestration.md — Give the drift-check command verbatim instead of prohibiting sed

- Added: 2026-08-24
- Evidence: Step 1 warns that the drift-check extraction must not post-process paths through sed, because on Windows the call exits empty and reads exactly like a valid "no drift" answer. The parent nonetheless improvised a find piped through a sed backslash-to-slash substitution, which failed with an unterminated-command parse error and printed a file count of 0 - the precise failure the prose predicts. The prohibition is stated but the correct command is not, so there is still something to improvise. The retry (find with no post-processing, a `grep -o` extraction over the tracker, and `comm` in both directions) worked first time and reported 93 files on disk against 98 tracked rows with 0 untracked drift.
- Proposed change: In `orchestration.md` step 1, replace the sed prohibition with the working command block verbatim - find for the filesystem side, a `grep -o` ERE over the tracker's Source column, `sort -u`, and `comm -23`/`comm -13` for both directions - and keep the "state the two counts" requirement.
