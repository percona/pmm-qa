# .claude/skills/codeceptjs-migration/SKILL.md - the "never add @nightly to the Playwright alternation" rule is stale against main

- Added: 2026-09-15
- Applies to: `.claude/skills/codeceptjs-migration/SKILL.md` ("Workflow coverage")
- Evidence: SKILL.md states "Never add `@nightly` to the Playwright alternation: that tag also marks tests owned by jobs whose services the nightly shards never provision." `git show -M 60e0e680 -- .github/workflows/nightly-e2e-tests-matrix.yml` (merged row-9 PR #1417) shows the maintainer collapsed `test_execution_playwright` to `tags_for_tests: '@nightly'` and deleted the in-file comment that carried the rule. So `@nightly` is the Playwright nightly bucket on `main` today. Row 10 satisfied the rule trivially - its scenario already carried `@nightly`, so nothing was added and no YAML changed - but the next row that genuinely needs an alternation edit will read a prohibition the repository has already abandoned, and either add a redundant tag or stop on a contradiction. Found independently by the runner and the final gate.
- Proposed change: Rewrite that sentence against the current `main`. The underlying hazard (a Playwright job selecting tests whose services the nightly shards never provision) is still real and still worth stating, but it is now a check to perform on what `@nightly` selects, not a prohibition on the tag. Confirm the current state of the alternation before rewriting, since it changed once already.
