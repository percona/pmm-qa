# .claude/skills/codeceptjs-migration/branch-workflow.md — `codeceptjs dry-run` cannot answer the emptying check on this machine

- Added: 2026-09-04
- Applies to: `.claude/skills/codeceptjs-migration/branch-workflow.md` ("Workflow coverage")
- Evidence: Step 5b tells the runner to confirm the emptying check "with `--grep` against the CodeceptJS suite". On row 5 that was impossible: `codeceptjs-e2e/node_modules` holds CodeceptJS 3.6.2 while `codeceptjs-e2e/package.json` declares `^4.1.0`, so `profile_test.js`'s `require('codeceptjs/effects')` throws `MODULE_NOT_FOUND` during discovery and kills the entire listing, not just that one file. Pre-existing and unrelated to any migration. The runner substituted a regex harness applying each consumer job's grep expression to scenario titles, which answers the same question without running CodeceptJS.
- Proposed change: Note the install skew and name the fallback, so the next runner does not spend its phase trying to repair `node_modules` — an out-of-scope change to a shared checkout that no migration should be making. Pair this with the tagged-versus-file counting lesson, because the fallback harness is exactly where that distinction has to be got right.
