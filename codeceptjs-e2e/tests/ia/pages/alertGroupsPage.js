const assert = require('assert');

const { I } = inject();

module.exports = {
  url: 'graph/alerting/groups',
  elements: {
    groupByContainer: '$group-by-container',
  },
  buttons: {
  },
  messages: {
  },
};
